[pytest]
testpaths = backend/tests tests
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# backend — for `from app.*` imports
# .       — for `from agents.*` imports (top-level agents/ package)
pythonpath = backend .
